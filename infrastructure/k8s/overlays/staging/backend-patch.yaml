apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  destination:
    namespace: staging
  replicas: 2
  template:
    spec:
      containers:
        - name: backend
          imagePullPolicy: IfNotPresent # prod / DOCR
          env:
            - name: GIN_MODE
              value: "release"
