apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../01-backend
  - ../../02-frontend
  - ../../03-redis
  - ../../04-postgres
  - gmail-credentials.sealed.dev.yaml

labels:
  - includeSelectors: true
    pairs:
      env: dev
patches:
  - path: backend-patch.yaml
